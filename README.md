# skills
AI Skills

## Skills Sync
Use `scripts/skills-sync.mjs` to sync skills from upstream repos into `skills/` based on `skills.config.json`.

Basic update:
```bash
npm run skills:sync
```

Clean local skills:
```bash
npm run skills:clean
```
